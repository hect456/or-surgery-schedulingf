# Core: OR-Tools provides both the MILP baseline (linear_solver, with CBC
# and SCIP bundled in the wheel — no external solver binary needed) and the
# interval-based CP-SAT production model (sat.python.cp_model).
ortools>=9.9

# Core: Gantt-style schedule plot (--plot flag). The case prompt explicitly
# allows "a simple image of the schedule" in lieu of a front end.
matplotlib>=3.8

# Optional: commercial backend for the baseline MILP (best performance at
# scale). Requires a Gurobi licence; OR-Tools' MPSolver drives it directly.
# gurobipy

# Optional: Hexaly local-search/hybrid backend (third point of the
# trade-off comparison). Requires an academic licence — see
# src/solvers/hexaly_solver.py for setup instructions.
# hexaly

# Optional: IBM ILOG CP Optimizer backend — a second CP engine, not a
# re-skin of CP-SAT (sequence-dependent room turnover via a transition
# matrix; see FORMULATION.md Appendix C). Requires a CP Optimizer engine
# (local CPLEX Studio or DOcplexcloud) plus a licence — see
# src/solvers/cp_optimizer_solver.py for setup instructions.
# docplex
